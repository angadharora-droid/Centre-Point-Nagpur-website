# Public Railway origin only; never put secrets in frontend settings.
PUBLIC_API_BASE_URL=http://localhost:3001

# Final canonical HTTPS domain. Vercel production URL is used when unset.
PUBLIC_SITE_URL=
