# Standalone API only (npm run test:backend / node backend/server.mjs).
# The deployed Railway service serves the API through scripts/serve.mjs instead.
PORT=3001
FRONTEND_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
